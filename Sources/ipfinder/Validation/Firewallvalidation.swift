class Firewallvalidation {

	/// Helper method for validating an IPFINDER Firewall and Format
	///
	/// - parameter by: AS number as (e.g. AS1) or country ISO 3166-1 alpha-2 country code (e.g. US)
	/// - parameter format: formats supported are apache_allow, apache_deny,nginx_allow,nginx_deny, CIDR, linux_iptables, netmask, inverse_netmask, web_config_allow, web_config_deny, cisco_acl, peer_guardian_2, network_object, cisco_bit_bucket, juniper_junos, microtik
    static func validate(_ by: String,format: String) {
       
    }
}